# Play_Web

1. We can query the airports and runway by using country name and code as below

localhost:9000/query/ai(country_code)

2. We can get different report with reports controllers which give us highest, lowest airport, runway surface, and runway identifications by calling below controller.

localhost:9000/reports
