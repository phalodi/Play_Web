# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

GET     /                        controllers.AirportController.home

GET     /query/:name                  controllers.AirportController.query(name)

GET     /reports                controllers.AirportController.reports
